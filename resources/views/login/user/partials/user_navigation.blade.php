<div class="text-center">
<a href="{{route('login.user_recents',$user->id)}}" class="btn btn-primary">Recent</a>
<a href="{{route('login.user_collections',$user->id)}}" class="btn btn-primary">Collections</a>
<a href="{{route('login.user_friends',$user->id)}}" class="btn btn-primary">Following</a>
</div>